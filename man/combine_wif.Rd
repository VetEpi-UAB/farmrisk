% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_wif.R
\name{combine_wif}
\alias{combine_wif}
\title{Combine What-If Scenarios}
\usage{
combine_wif(data, scenario = NULL, wif_list = NULL, wif_only = FALSE)
}
\arguments{
\item{data}{Data frame containing mcnodes input data}

\item{scenario}{Character string naming the combined scenario. If NULL, constructed from wif_list}

\item{wif_list}{Character vector of what-if function names to apply in sequence}

\item{wif_only}{Logical indicating whether to return only the new scenario (TRUE) or all scenarios (FALSE)}
}
\value{
Data frame with either:
\itemize{
\item If wif_only=FALSE: Original data plus new wif scenario rows
\item If wif_only=TRUE: Only the new wif scenario rows
}
}
\description{
Applies multiple what-if (wif) functions in sequence to generate combined scenarios
}
\details{
Combine scenarios by applying multiple what-if functions sequentially
}
\examples{
combine_wif(
  data = quarantine_data,
  scenario = "Test during quarantine with exclusive material",
  wif_list = c("quarantine", "exclusive_material", "test")
)
}
